{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma db push && node prisma/seed.js && next build",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/**/route.ts": {
      "maxDuration": 30
    }
  }
}
